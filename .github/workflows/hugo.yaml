name: Deploy Hugo site to Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.151.0
    steps:
      # 1.Checkout the content repository (no submodules)
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false   # ← 关闭子模块
      # 2.Install Go (required for Hugo Modules)
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.20"
      # 3.Install Hugo CLI
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
      # 4.Install Dart Sass
      - name: Install Dart Sass
        run: sudo snap install dart-sass
      # 5.Initialize Hugo Modules
      - name: Initialize Hugo Modules
        run:  |
          hugo mod init github.com/${{ github.repository }}
          hugo mod get -u
      # 6.Setup Pages
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4
      # 7.Install Node.js dependencies (optional)
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"
      # 8.Build with Hugo
      - name: Build with Hugo
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
          TZ: America/Los_Angeles
        run: |
          hugo \
            --gc \
            --minify \
            --ignoreCache \
            --buildFuture \
            --baseURL "${{ steps.pages.outputs.base_url }}/"
      # Upload artifact for deployment job   
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4